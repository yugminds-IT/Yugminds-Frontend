# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: leaves.spec.ts >> Teacher — Leaves >> submitting a leave request through the real form shows it in Leave History
- Location: e2e/teacher/leaves.spec.ts:10:7

# Error details

```
Test timeout of 45000ms exceeded.
```

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.border.rounded-lg').filter({ hasText: 'QA e2e leave reason 1784690667124' })
Expected: visible
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('.border.rounded-lg').filter({ hasText: 'QA e2e leave reason 1784690667124' })

```

```yaml
- text: TP Teacher Portal Teacher
- button "Collapse sidebar"
- textbox "Search..."
- navigation:
  - list:
    - listitem:
      - button "Dashboard"
    - listitem:
      - button "My Classes"
    - listitem:
      - button "Submit Report"
    - listitem:
      - button "Attendance"
    - listitem:
      - button "Leave Requests"
    - listitem:
      - button "Notifications"
    - listitem:
      - button "Analytics"
    - listitem:
      - button "Student Progress"
    - listitem:
      - button "Assignments"
    - listitem:
      - button "Settings"
- text: QT
- paragraph: QA Teacher 0
- paragraph: __qa_test_mrvifuv9ls1l___teacher0@example.test
- button "Logout"
- heading "Leave Requests" [level=1]
- paragraph: Apply for leave or view your leave history for __qa_test_mrvifuv9ls1l__ School
- text: Apply for Leave Submit a leave request. Your School Admin and System Admin will review and approve or reject it. Start Date *
- textbox "Start Date *"
- text: End Date *
- textbox "End Date *"
- text: Reason for Leave *
- textbox "Reason for Leave *":
  - /placeholder: Please provide a reason for your leave request...
- checkbox "Substitute teacher required"
- text: Substitute teacher required
- button "Submit Leave Request" [disabled]
- button "Cancel"
- text: Leave History Your recent leave requests
- paragraph: No leave requests yet
- alert
- region "Notifications":
  - alert:
    - paragraph: Leave request submitted successfully! It will be reviewed by your School Admin and System Admin.
    - button "Dismiss notification"
```

# Test source

```ts
  1  | import { test, expect } from './base';
  2  | 
  3  | function isoDate(daysFromToday: number): string {
  4  |   const d = new Date();
  5  |   d.setDate(d.getDate() + daysFromToday);
  6  |   return d.toISOString().split('T')[0];
  7  | }
  8  | 
  9  | test.describe('Teacher — Leaves', () => {
  10 |   test('submitting a leave request through the real form shows it in Leave History', async ({ page }) => {
  11 |     await page.goto('/lms/teacher/leaves');
  12 |     await expect(page.getByRole('heading', { name: 'Leave Requests' })).toBeVisible();
  13 | 
  14 |     const uniqueReason = `QA e2e leave reason ${Date.now()}`;
  15 |     await page.locator('#start_date').fill(isoDate(1));
  16 |     await page.locator('#end_date').fill(isoDate(2));
  17 |     await page.locator('#reason').fill(uniqueReason);
  18 | 
  19 |     const createResponse = page.waitForResponse(
  20 |       (res) => res.url().endsWith('/teacher/leaves') && res.request().method() === 'POST',
  21 |     );
  22 |     await page.getByRole('button', { name: 'Submit Leave Request' }).click();
  23 |     const createRes = await createResponse;
  24 |     expect(createRes.ok(), await createRes.text()).toBeTruthy();
  25 | 
  26 |     const leaveCard = page.locator('.border.rounded-lg', { hasText: uniqueReason });
> 27 |     await expect(leaveCard).toBeVisible({ timeout: 10000 });
     |                             ^ Error: expect(locator).toBeVisible() failed
  28 |     await expect(leaveCard.getByText('Pending')).toBeVisible();
  29 |   });
  30 | });
  31 | 
```